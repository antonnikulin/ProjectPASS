{
    "name": "project-pass",
    "version": "0.1.0",
    "main": "core.js",
    "scripts": {
        "build": "electron-packager . ProjectPASS --platform=win32 --arch=x64 --version=1.3.9 --app-version=0.1.0 --out=dist --ignore=dist --prune --icon=./view/assets/icons/favicon.ico --overwrite"
    }
}
